//This file is responsible for exporting react-router's history module so that auth0 can redirect the user programmatically
import { createBrowserHistory } from "history";

export default createBrowserHistory();